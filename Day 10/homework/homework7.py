# append(value) — დაამატეთ ელემენტი ბოლოს
# insert(index, value) — ჩასვით ელემენტი კონკრეტულ პოზიციაზე
# extend(iterable) — გაერთიანება სხვა iterable-ს ელემენტებით
# remove(value) — წაშალეთ პირველი ელემენტი მნიშვნელობით
# pop(index) — წაშალეთ ელემენტი ინდექსით (თუ არ მიუთითეთ — ბოლო)
# index(value) — ელემენტის ინდექსის პოვნა
# count(value) — ელემენტის რაოდენობის დათვლა
